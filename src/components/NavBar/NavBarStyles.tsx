export const styles = (theme) => {
// export const styles = {
  return ({
    container: {
      display: "flex",
      flexDirection: "column",
    },
    text: {
      color: theme.text,
    }
  });
}
