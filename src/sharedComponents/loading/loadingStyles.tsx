const styles = (theme) => {
  return ({
    container: {
      backgroundColor: theme.backgroundPrimary,
      display: "flex",
      justifyContent: "center",
      alignContent: "center",
      alignItems: "center",
    },
  });
}

export default styles;
