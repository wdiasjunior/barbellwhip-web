const styles = (theme) => {
  return ({
    container: {
      backgroundColor: theme.backgroundPrimary,
      flex: 1,
      justifyContent: "center",
      alignContent: "center",
      alignItems: "center",
    },
  });
}

export default styles;
